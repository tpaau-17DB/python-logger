import logger as l

i = 0
while i < 4:
    print(f"\ntesting for verbosity = {i}")
    l.set_verbosity(i)
    l.log_deb("debug")
    l.log_mess("info")
    l.log_warn("warning")
    l.log_err("error")
    i += 1
